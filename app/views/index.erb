<h1>URLs too short?</h1>
<h2>NOT A PROBLEM!!</h2>
<h2>We'll longen that URL for you. Just enter it here:</h2>
<form action="/your_custom_really_really_long_url" method="POST">
  <input type="text" name="url[original_url]">
  <input type="submit" value="LONGEN IT">
</form>

<h3>Brilliant longened URLs of the past</h3>
<ul>
  <% @urls.each do |url| %>
  <li>
    <a href="<%= url.original_url %>">
      <%= url.original_url %>
    </a>
  </li>
  <li>
    <a href="<%= "#{url.long_url_hash}" %>">
      <%= "http://localhost.9393/#{url.long_url_hash}" %>
    </a>
  </li>
  <br />

  <% end %>
</ul>
